# INCLUDE host_boot — agent_boot + host_session one-shot

HOLD_FLASH 1

DEFAULT NEED_PATHS = ""
DEFAULT NEED_PATH_ANY = ""
DEFAULT PATH_GUARD_SOFT = 0
DEFAULT NEED_READ_PATHS = ""
DEFAULT NEED_WRITE_PATHS = ""
DEFAULT RW_GUARD_SOFT = 0
DEFAULT NEED_BINS = ""
DEFAULT NEED_BIN_ANY = ""
DEFAULT BIN_GUARD_SOFT = 0
DEFAULT NEED_LIBS = ""
DEFAULT NEED_LIB_ANY = ""
DEFAULT LIB_GUARD_SOFT = 0
INCLUDE host_boot
ASSERT OK == 1
ASSERT HOST_SESSION_OK == 1
HASINCLUDE agent_boot
ASSERT LAST_N == 1
HASINCLUDE host_session
ASSERT LAST_N == 1
HASINCLUDE host_boot
ASSERT LAST_N == 1
HASLIB host_boot
ASSERT LAST_N == 1
VERSION
ASSERT LAST != ""

LET NEED_LIBS = "nosuch_host_boot_miss"
LET LIB_GUARD_SOFT = 1
INCLUDE host_boot
ASSERT HOST_SESSION_OK == 0
CLEAR_ERR

LET NEED_LIBS = "agent_boot"
LET LIB_GUARD_SOFT = 0
INCLUDE host_boot
ASSERT HOST_SESSION_OK == 1

PASS
PRINT "1398_host_boot: PASS"
