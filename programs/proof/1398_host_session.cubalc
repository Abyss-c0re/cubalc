# INCLUDE host_session — path+rw+bin+lib combined host contract

HOLD_FLASH 1

DEFAULT NEED_PATHS = ""
DEFAULT NEED_PATH_ANY = ""
DEFAULT PATH_GUARD_SOFT = 0
DEFAULT NEED_READ_PATHS = ""
DEFAULT NEED_WRITE_PATHS = ""
DEFAULT RW_GUARD_SOFT = 0
DEFAULT NEED_BINS = ""
DEFAULT NEED_BIN_ANY = ""
DEFAULT BIN_GUARD_SOFT = 0
DEFAULT NEED_LIBS = ""
DEFAULT NEED_LIB_ANY = ""
DEFAULT LIB_GUARD_SOFT = 0
INCLUDE host_session
ASSERT HOST_SESSION_OK == 1
ASSERT PATH_GUARD_OK == 1
ASSERT RW_GUARD_OK == 1
ASSERT BIN_GUARD_OK == 1
ASSERT LIB_GUARD_OK == 1
ASSERT OK == 1

# soft ALL path miss → host not ok
LET NEED_PATHS = "nosuch_cubalc_host_path_miss"
LET PATH_GUARD_SOFT = 1
INCLUDE host_session
ASSERT PATH_GUARD_OK == 0
ASSERT HOST_SESSION_OK == 0
CLEAR_ERR

# restore empty + soft hit
LET NEED_PATHS = ""
LET PATH_GUARD_SOFT = 0
LET NEED_LIBS = "agent_boot"
LET LIB_GUARD_SOFT = 0
INCLUDE host_session
ASSERT LIB_GUARD_OK == 1
ASSERT HOST_SESSION_OK == 1

# soft lib miss
LET NEED_LIBS = "nosuch_cubalc_host_lib"
LET LIB_GUARD_SOFT = 1
INCLUDE host_session
ASSERT LIB_GUARD_OK == 0
ASSERT HOST_SESSION_OK == 0
CLEAR_ERR

HASINCLUDE host_session
ASSERT LAST_N == 1
HASLIB host_session
ASSERT LAST_N == 1

PASS
PRINT "1398_host_session: PASS"
