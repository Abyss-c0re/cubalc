# RELATED / SEEALSO — form neighbor bag (dual of FORMSFOR topic bags)

RELATED HASFORM
ASSERT OK == 1
ASSERT RELATED_OF == "HASFORM"
ASSERT RELATED_N >= 5
ASSERT LAST_N >= 5
SYS HASI "NEEDFORM"
ASSERT LAST_N == 1
SYS HASI "FORMHINT"
ASSERT LAST_N == 1
SYS HASI "LISTFORMS"
ASSERT LAST_N == 1

RELATED SETP
ASSERT OK == 1
ASSERT RELATED_OF == "SETP"
SYS HASI "GETP"
ASSERT LAST_N == 1
SYS HASI "NEEDP"
ASSERT LAST_N == 1
SYS HASI "SAVEPLATE"
ASSERT LAST_N == 1

# alias
SEEALSO INCLUDE
ASSERT OK == 1
ASSERT RELATED_OF == "INCLUDE"
SYS HASI "LISTLIBS"
ASSERT LAST_N == 1
SYS HASI "RECIPE"
ASSERT LAST_N == 1

RELATED ASSERT
ASSERT OK == 1
SYS HASI "EXPECT"
ASSERT LAST_N == 1
SYS HASI "WHY"
ASSERT LAST_N == 1

RELATED TIPS
ASSERT OK == 1
SYS HASI "FORMSFOR"
ASSERT LAST_N == 1

# miss soft
RELATED ZZZ_NO_SUCH_FORM
ASSERT OK == 0
CLEAR_ERR

# need name
RELATED
ASSERT OK == 0
CLEAR_ERR

PASS
PRINT "1331_related: PASS"
