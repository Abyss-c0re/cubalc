# CubeOS way — CubalC

**The Cube is sovereign. OS is only the path (way).**  
**Core = braincube** (State Matrix → algocube digit → `DECIDE`).

```bash
export PROPHECY_CUBE_ROOT=~/Dev/lab/prophecy_cube
export CUBALC_STATE=$PROPHECY_CUBE_ROOT/state
export HOLD_FLASH=1

# boot full CubeOS lattice in CubalC (publishes viz for LOVR)
./out/cubalc boot

# one energy+decide tick
./out/cubalc os tick

# continuous flow (no flash)
bash scripts/cubeos_flow.sh
```

Law: HOLD_FLASH · share matrix only · devices free · one Commander · C3.
