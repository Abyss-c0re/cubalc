# CubalC Justice — Project Nexus accordance

**Justice** = Cube Law realized as SMX-verifiable state, not speech.

## Vision (abstract)

| Token | Law |
|-------|-----|
| `sot` | Cube is source of truth |
| `hive` | Shared board, durable handoff |
| `no_pii` | Matrix only — no personal data |
| `fail_closed` | Integrity fails closed |
| `one_cmd` | One Commander (crypto seal) |
| `devices_free` | Recycle devices; user sovereign |
| `manifest_smx` | Peers manifest by SMX exchange |
| `abstract_lang` | No product brands in language/core |
| `no_http` | Core talk never requires HTTP |
| `hold_flash` | Never auto-flash |
| `per_object` | Viz = per-object render plan |

## Manifest

```bash
export CUBALC_STATE=./state
./scripts/manifest_justice_nexus.sh
```

Program: `programs/prophecy/justice_nexus.cubalc`  
Plate: `state/CUBE_JUSTICE_NEXUS.json` → Project Nexus `var/`

## Proofs included

`law` · `smx-selftest` · `smx-exchange` · `smx-bus prove` · hive braincube · justice board

Token `C3` · Share `smx` · Proto `SMX2` · Version `1.4.2-c3`
